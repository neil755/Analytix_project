<?xml version="1.0" ?>
<odoo>
    <data noupdate="1">
        <record id="email_template_escalation_tracker" model="mail.template">
            <field name="name">Escalation Assignment Notification</field>
            <field name="email_from">${(object.company_id.email or 'noreply@yourcompany.com')}</field>
            <field name="subject">[Escalation Tracker] Task Assigned - ${object.partner_id.name}</field>
            <field name="model_id" ref="project_custom.model_escalation_sheet"/>
            <field name="auto_delete" eval="True"/>
            <field name="email_to">${object.reassign_level2.email or object.reassign_level3.email
                                     or object.reassign_level4.email or object.reassign_level5.email}</field>
            <field name="body_html">
                <![CDATA[
                <p>Dear ${object.reassign_level2.name or object.reassign_level3.name
                            or object.reassign_level4.name or object.reassign_level5.name},</p>
                <p>You have been assigned a new escalation task.</p>
                <p><strong>Customer:</strong> ${object.partner_id.name or 'N/A'}</p>
                <p><strong>Project:</strong> ${object.project_id.name or 'N/A'}</p>
                <p><strong>Priority:</strong> ${object.priority or 'N/A'}</p>
                <p>Please review the task at your earliest convenience.</p>
                <p>Best regards,</p>
                <p>${object.company_id.name}</p>
                ]]>
            </field>
        </record>
    </data>
</odoo>
